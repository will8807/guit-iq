import { describe, it, expect } from "vitest";

// Placeholder test — confirms the Vitest + RTL setup works.
// Will be replaced by real tests in Milestone 1+.
describe("sanity check", () => {
  it("true is true", () => {
    expect(true).toBe(true);
  });

  it("basic arithmetic works", () => {
    expect(2 + 2).toBe(4);
  });
});
